# Conflux Chrome 扩展（桌面整理端）

浏览器里负责：**存、找、整理**。深读、标注、视频仍以手机 App 为主。

## 安装（开发者模式）

1. 打开 Chrome → `chrome://extensions`
2. 打开右上角「开发者模式」
3. 「加载已解压的扩展程序」，选本目录 `extension/`
4. 点工具栏图标，用和 App 相同的手机号登录
5. 改完代码后，在扩展页点「重新加载」

## 用法

- **工具栏图标**：从浏览器右侧打开面板（类似 MetaMask）
- **首页**：未读 / 最近阅读 / 漫游；搜索；加号添加链接（当前页或粘贴 URL）
- **我的收藏**：系统分类（含回收站）/ 标签；右键删除自建标签
- **详情**：标题、解析状态、封面/正文预览；⋯ 复制链接 / 删除。点链接打开原文。阅读、标注仍在手机 App
- **右键页面 / 链接**：「收藏到 Conflux」
- **快捷键**：`Alt + S`（可在 `chrome://extensions/shortcuts` 改）

登录态存在本机 Chrome，与手机 App 是同一套账号，不是同一台设备的会话。

## 说明

请求发往 `https://inkmind.xyz/collection`（与 App 相同）。改地址编辑 `config.js` 后重新加载扩展。
